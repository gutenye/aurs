pkgname="gututils"
pkgver=0.0.0
pkgrel=1
pkgdesc="Guten's cmdline utils"
arch=("any")
url="https://github.com/gutenye/gututils"
license=("MIT")
md5sums=()

build() {
}

package() {
  mkdir -p "$pkgdir/usr/bin"
  cp aur "$pkgdir/usr/bin"
}
