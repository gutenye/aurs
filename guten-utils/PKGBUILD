# Maintainer: Guten Ye

pkgname="guten-utils"
pkgver=20120911
pkgrel=2
pkgdesc="my cmdline utils"
arch=("any")
url="https://github.com/GutenYe/guten-utils"
license=("MIT")

build() {
  cd "$srcdir"
	./build.sh
}

package() {
  mkdir -p "$pkgdir/usr/bin"
  cp dist/* "$pkgdir/usr/bin"
}
md5sums=()
